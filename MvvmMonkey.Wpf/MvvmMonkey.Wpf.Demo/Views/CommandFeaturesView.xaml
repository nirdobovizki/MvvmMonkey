<UserControl x:Class="NirDobovizki.MvvmMonkey.Wpf.Demo.Views.CommandFeaturesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NirDobovizki.MvvmMonkey.Wpf.Demo.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <CheckBox IsChecked="{Binding Enabled}" Content="Enabled"/>
            <Button Command="{Binding EnableCommand}" Content="Enable"/>
            <Button Command="{Binding DisableCommand}" Content="Disable"/>
            <Button Command="{Binding RefreshCommand}" Content="Refresh"/>
        </StackPanel>
    </Grid>
</UserControl>
